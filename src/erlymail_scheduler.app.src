{application, erlymail_scheduler,
    [
        {description, "Stores emails to be sent at a later time."},
        {vsn, "0.1.0"},
        {registered, []},
        {applications,
            [
                kernel,
                stdlib,
                crypto,
                public_key,
                ssl
            ]
        },
        {mod, { erlymail_scheduler_app, []}},
        {env, []}
    ]
}.
